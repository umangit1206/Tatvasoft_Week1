import React from 'react'

function Array({array,key}) {
  return (
    <div>
      <h2>Id {array.id} Title {array.title} Description {array.description}</h2>
    </div>
  )
}

export default Array


